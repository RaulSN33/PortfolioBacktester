[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "PortfolioBacktester"
version = "0.1.0"
dependencies = [
    "pandas",
    "matplotlib",
    "numpy",
    "scipy",
]

[tool.setuptools.packages.find]
where = ["src"]
